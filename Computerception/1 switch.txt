// Switches here are substitute for transistors
// Switches depends on states
// Switches can have 2 states (binary)
  // If switch is ON, then it will output the ON-state
  // If switch is OFF, then it will output the OFF-state